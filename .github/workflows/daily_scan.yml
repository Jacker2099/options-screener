name: NVDA TSLA Post-Close Options Radar

on:
  schedule:
    # GitHub Actions schedule can be delayed. Trigger multiple times across 00-02 UTC
    # and let the script accept only the ET 20:00-21:59 post-close window.
    - cron: "17,47 0-2 * * 2-6"
    - cron: "30 12 * * 0"
  workflow_dispatch:

concurrency:
  group: nvda-tsla-options-radar
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  PYTHONUNBUFFERED: "1"

jobs:
  radar:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install yfinance pandas requests

      - name: Run Radar
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          set -euo pipefail
          mkdir -p data/options_snapshots reports/daily reports/weekly
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            python options_screener.py --mode daily 2>&1 | tee reports/last_run.log
          else
            python options_screener.py --mode auto --enforce-postclose-window --skip-if-trade-date-exists 2>&1 | tee reports/last_run.log
          fi

      - name: Upload Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nvda-tsla-options-radar-${{ github.run_number }}
          path: |
            data/options_history.sqlite
            data/options_snapshots/*
            reports/daily/*
            reports/weekly/*
            reports/last_run.log
          retention-days: 30
          if-no-files-found: warn

      - name: Commit Data
        if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
        continue-on-error: true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data reports || true
          git diff --staged --quiet && exit 0
          git commit -m "options radar $(date -u +'%Y-%m-%d')"
          git pull --rebase origin "${{ github.ref_name }}" || true
          git push || echo "git push skipped, workflow continues"
